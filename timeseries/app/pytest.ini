[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: marks tests requiring raster files or network (deselect with -m "not integration")
